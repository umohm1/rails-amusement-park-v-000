<% unless current_user.admin %>
  <%= button_to "Go on this ride", rides_path({ride: {user_id: current_user.id, attraction_id: @attraction.id}}) %>
   <% else %>
  <%= link_to "Edit Attraction", edit_attraction_path(@attraction.id) %>
<% end %>
<p><%= @attraction.name %></p>
